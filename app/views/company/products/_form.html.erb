<%= simple_form_for [@company, @product] do |f| %>
	<%= f.input :name %>
	<%= f.input :price, required: true %>
	<%= f.input :description %>
	<%= f.input :status, collection: Product::STATUSES, include_blank: false, required: true %>
	<%= f.association :category %>
	<%= f.input :product_picture %>

	<%= f.button :submit %>
<% end %>